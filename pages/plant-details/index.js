export default function PlantList() {
  return (
    <>
      {/* <div>
        <button>
          <Link href="../">Home</Link>
        </button>
        <h1>Plants</h1>
        <ul>
          {plants.map((plant) => (
            <li key={plants.id}>
              <Link href={`/plants/${plant.slug}`}>{plant.name}</Link>
            </li>
          ))}
        </ul>
      </div>
      ; */}
    </>
  );
}
